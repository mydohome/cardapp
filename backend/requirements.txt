fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
# Richiesto da pydantic.EmailStr (usato in schemas.py): senza questo pacchetto
# l'app non si avvia proprio (ImportError al primo import di schemas.py).
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 non e' compatibile con bcrypt >= 4.1 (rimuove un attributo
# interno che passlib usa per rilevare la versione): senza questo pin,
# ogni hash/verifica password fallisce con "password cannot be longer
# than 72 bytes". Vedi https://github.com/pyca/bcrypt/issues/684
bcrypt==4.0.1
python-multipart==0.0.9
redis==5.0.8
Pillow==10.4.0
pytesseract==0.3.13
pyzbar==0.1.9
